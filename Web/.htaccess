# 기본 옵션 설정
Options -Indexes
Options -MultiViews

# PHP 오류 표시 설정
php_flag display_errors off

# 기본 문자셋 설정
AddDefaultCharset UTF-8

# script.js 파일 접근 차단
<Files "script.js">
    Order allow,deny
    Deny from all
</Files>

# 파일 접근 권한 확인
<Directory "/var/www/html">
    Require all granted
</Directory>

# 디렉토리 보안 설정
; <Directory "/Question/question6">
;     Options -Indexes
;     Order allow,deny
;     Allow from all
;     <FilesMatch "\.js$">
;         Order deny,allow
;         Deny from all
;     </FilesMatch>
; </Directory> 